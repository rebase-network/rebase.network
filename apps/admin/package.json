{
  "name": "@rebase/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host localhost --port 5174 --strictPort",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview --host localhost --port 4174 --strictPort",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@rebase/shared": "workspace:*",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@vitejs/plugin-vue": "^5.2.3",
    "typescript": "^5.8.2",
    "vite": "^6.2.1",
    "vue-tsc": "^2.2.8"
  }
}
