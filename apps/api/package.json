{
  "name": "@rebase/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @rebase/shared build && pnpm --filter @rebase/db build && node --env-file=../../.env --watch --import tsx src/index.ts",
    "build": "pnpm --filter @rebase/shared build && pnpm --filter @rebase/db build && tsc -p tsconfig.json",
    "start": "node --env-file=../../.env dist/index.js",
    "typecheck": "pnpm --filter @rebase/shared build && pnpm --filter @rebase/db build && tsc -p tsconfig.json --noEmit",
    "bootstrap-admin": "node --env-file=../../.env --import tsx src/scripts/bootstrap-admin.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.1",
    "@rebase/db": "workspace:*",
    "@rebase/shared": "workspace:*",
    "better-auth": "^1.3.12",
    "drizzle-orm": "^0.41.0",
    "hono": "^4.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
